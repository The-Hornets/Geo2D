# frozen_string_literal: true

RSpec.describe Geo2d do
  it "has a version number" do
    expect(Geo2d::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
